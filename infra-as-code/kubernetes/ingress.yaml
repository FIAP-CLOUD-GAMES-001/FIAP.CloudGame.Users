apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fcg-users-api-ingress
  annotations:
    konghq.com/strip-path: "true"
spec:
  ingressClassName: kong
  rules:
    - http:
        paths:
          - path: /users
            pathType: Prefix
            backend:
              service:
                name: fcg-users-api-service
                port:
                  number: 80